<ul>
{% for link in linklists.main-menu.links %}
  <li>{{ link.title | link_to: link.url, link.title }}</li>
{% endfor %}
</ul>

{% assign my_name = 'hello world' %}

<h3>{{ my_name.size }}</h3>

<h2>{{paginate.page_size}}</h2>
{{ content_for_index }}
